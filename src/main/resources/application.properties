# Puerto y nombre del microservicio
server.port=8090
spring.application.name=catalogo-service

# Conexión a NeonTech (PostgreSQL)
spring.datasource.url=jdbc:postgresql://ep-dawn-poetry-apqfw71e-pooler.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_HicOqfERJz76
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# 🛰️ Conexión Inteligente al Servidor Eureka
# Si la variable EUREKA_URL existe (en Railway), la usará. Si no (en local), usará localhost.
eureka.client.service-url.defaultZone=${EUREKA_URL:http://localhost:8761/eureka/}